import { eyebrowClass, panelClass } from "@/lib/ui";
export function ColdWalkInAnalytics({ walkIns, members }: { walkIns: Array<{ wasSold: boolean; participantIds: string[] }>; members: Array<{ id: string; name: string }> }) {
  const rows = [{ id: "all", name: "Toplam", items: walkIns }, ...members.map((member) => ({ id: member.id, name: member.name, items: walkIns.filter((item) => item.participantIds.includes(member.id)) }))];
  return <section className={`${panelClass} mt-6`}><div className="mb-5"><p className={eyebrowClass}>Cold walk-in analizi</p><h2 className="mt-1 text-xl font-black">Saha başarı oranı</h2></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{rows.map((row) => { const sold = row.items.filter((item) => item.wasSold).length; const failed = row.items.length - sold; const percent = row.items.length ? Math.round(sold / row.items.length * 100) : 0; return <article key={row.id} className="rounded-2xl bg-slate-50 p-4"><p className="font-black">{row.name}</p><p className="mt-2 text-2xl font-black">{row.items.length}</p><div className="mt-3 grid grid-cols-2 gap-2 text-xs"><p className="font-bold text-emerald-700">{sold} · %{percent}<br/><span className="text-slate-400">Başarılı</span></p><p className="font-bold text-rose-600">{failed} · %{100-percent}<br/><span className="text-slate-400">Başarısız</span></p></div></article>; })}</div></section>;
}
